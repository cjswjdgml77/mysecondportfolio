const styles = {
  topLayout: "flex w-[100%] relative h-[100vh]",
  titleLayout: "w-full flex justify-center relative",
  slideLayout:
    "w-full absolute transition-all ease-in duration-500 translate-y-[0%]",
};
export const flex = {
  row: "w-full flex flex-row align-center justify-center text-center",
  col: "w-full flex flex-col align-center justify-center text-center",
};
export const section = {
  section: "h-[100vh] flex flex-col ",
};

export default styles;
